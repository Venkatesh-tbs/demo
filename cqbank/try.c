#include<stdio.h>
int main(){
    int  n, rev = 0 ,rem  ;
    printf("num :");
    scanf("%d",&n);
     while (copy > 0)
     {
        rem = n*10;
         =  rev + (copy%10);
        copy /= 10;
     }
     if (n==rev )
     {
        printf("is palindrome"); 
     }
     else
     {
        printf("not palindrome");
     }
}